bindec - переводит из двоичного числа в десятичное
implode - обьеденение элементы масива в одну строку
array - массив который нужно одьеденить
$c - каждый символ из $key
return decbin(num: ord(character: $c)); - возвращение двоичного представление символа $c
str_split - разбивание строки на массив символов
string - перед символом показывает что именно надо преобразовать
$ - обозначение какой либо переменной ($name = "Алексей"; // $name — это переменная, которая хранит строку "Алексей")
Переменная - контейнер в котором храняться данные (числа, стоки, массивы и тд)


